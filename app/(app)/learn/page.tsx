import { SelfPacedCoursesPageContent } from "@/modules/self-paced/components/SelfPacedCoursesPageContent";

export const metadata = { title: "Self-paced courses" };

export default function SelfPacedCoursesPage() {
  return <SelfPacedCoursesPageContent />;
}
