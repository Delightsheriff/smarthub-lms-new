# Assigned to you (`/assigned`)

Status: ✅ Design pass done — commit `5581a68`. Legacy-parity not yet audited.

`PageHeader`/`EmptyState` adopted. The accordion-card content (recordings/
materials/tasks sections per assigned module) was already well-built and
untouched. A dedicated legacy-parity pass hasn't been run on this surface
yet — if picking this back up, compare
`~/Documents/smarthub-projects/smarthub/smarthub-core-lms`'s equivalent
(search for "assigned" / "AssignedModules" under `src/modules`) the same way
the other surfaces in this folder were audited.
