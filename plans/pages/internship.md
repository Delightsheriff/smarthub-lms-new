# Internship (`/internships`, `/internships/me/payment`)

Status: ✅ Done — editorial PageHeader rolled out on workspace overview (Plan 016 Slice 6). Legacy-parity not yet audited.

`PageHeader`/`EmptyState` adopted on workspace + payment pages. Task
submission, check-ins, and payment-proof upload flows were already
well-built and untouched. No legacy-parity comparison run yet this
session — legacy's internship module (if one exists) hasn't been read.
