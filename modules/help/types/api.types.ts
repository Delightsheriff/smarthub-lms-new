import type { WireHelpResource } from "@/lib/api/wire.types";

/**
 * smarthub-api shapes for the Help library. Wire record re-exported so
 * help-side code reads through the module seam.
 */
export type ApiHelpResource = WireHelpResource;